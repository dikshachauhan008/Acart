import { FilterSellerProductsPipe } from './filter-seller-products.pipe';

describe('FilterSellerProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSellerProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
